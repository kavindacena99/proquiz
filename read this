Now we have to create the part of calculating the marks. We will calculate the marks based on the correct answers given by the user. We will also save the result to the server.

create leaderboard.php file and add leaderboard feature

errors showing


//from this we create the part of calculating marks

      /*
        // Save the result to the server
const resultData = {
  totalFaced: questions.length,
  correctAnswers: marks,
  category: category,
  language: lang,
  created: created
};

fetch('save_results.php', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(resultData)
})
  .then(response => response.text())
  .then(data => {
    console.log("Results saved:", data);
  })
  .catch(error => console.error("Error saving results:", error));


